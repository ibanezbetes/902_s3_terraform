name: 05-mi-repo

on:
    push:
      branches:
        - master
      paths:
        - web/**

jobs:
    user-repo:
        runs-on: ubuntu-latest
        steps:
            - name: Descargar repo
              uses: actions/checkout@v4
            - name: Mostrar contenido del repo
              run: |
                ls -la web